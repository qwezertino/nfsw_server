# ============================================================
#  SBRW Server Configuration
#  Copy this file to .env and fill in the values:
#    cp .env.example .env
# ============================================================

# --- Network ---
SERVER_IP=192.168.1.100
SERVER_PORT=4444

# --- Database (soapbox-race-core) ---
DB_HOST=localhost
DB_NAME=nfs_world
DB_USER=nfs_user
DB_PASS=qwerty123456

# --- Openfire ---
# Get this token from Openfire admin console → Server → Server Settings → REST API
OPENFIRE_TOKEN=changeme
OPENFIRE_DB_HOST=localhost
OPENFIRE_DB_NAME=openfire_nfs
OPENFIRE_DB_USER=nfs_user
OPENFIRE_DB_PASS=qwerty123456

# --- ModNet ---
MODDING_SERVER_ID=sbrw-private
